feedparser>=6.0.11
openai>=1.14.0
python-dateutil>=2.9.0
rapidfuzz>=3.6.0
requests>=2.31.0
python-dotenv>=1.0.0

# Optional: better named-entity recognition (falls back to regex without it)
# pip install spacy && python -m spacy download en_core_web_sm
# spacy>=3.7.0

# breaking_check.py, preferences_updater.py, and news_intel.remote_prefs use the packages above.
